<%= form_for([:manager, @rotating_image]) do |form| %>
  <% if rotating_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rotating_image.errors.count, "error") %> No se puede guardar:</h2>

      <ul>
      <% rotating_image.errors.full_messages.each do |message| %>
        <li class="alert alert-danger"><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="container divLogin">

      <div class="form-group">
        <label for="name" class="cols-sm-2 control-label">Descripción</label>
        <div class="cols-sm-10">
          <div class="input-group">
            <span class="input-group-addon"><i class="fa fa-pencil" aria-hidden="true"></i></span>
            <%= form.text_area :name %>
          </div>
        </div>
      </div>

      <div class="form-group" align="center">
        <label for="name" class="cols-sm-2 control-label">Imagen</label>
        <div class="cols-sm-10">
          <div class="input-group">
            <%= image_tag rotating_image.rotating_file.url(:medium) %>
          </div>
        </div>
      </div>

      <div class="form-group" align="center">
        <label for="name" class="cols-sm-2 control-label">Seleccione Imagen</label>
        <div class="cols-sm-10">
          <div class="input-group">
            <%= form.file_field :rotating_file %>
          </div>
        </div>
      </div>

      <div align="center">
        <%= form.submit 'Actualizar' , :class => 'btn9 editar', :style => 'color: white; text-decoration: none' %>
        <br/><br/>
      </div>
  </div>
<% end %>
