<%= form_with(model: rotating_image, local: true) do |form| %>
  <% if rotating_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rotating_image.errors.count, "error") %> prohibited this rotating_image from being saved:</h2>

      <ul>
      <% rotating_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
